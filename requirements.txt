# Core ML/Training dependencies
torch>=2.0.0
transformers>=4.35.0
peft>=0.6.0
accelerate>=0.24.0
datasets>=2.14.0
huggingface-hub>=0.16.0
tqdm>=4.65.0

# Optional: Experiment tracking (optional but recommended for training)
wandb>=0.15.0

# Data processing
numpy>=1.21.0
pillow>=9.0.0
pandas>=1.5.0

# Visualization
matplotlib>=3.5.0
seaborn>=0.12.0

# Inference client
openai>=1.0.0

# Cloud deployment
modal>=0.49.0

# Development and testing (optional)
pytest>=7.0.0
black>=22.0.0
flake8>=5.0.0

# Note: SGLang is not included here as it's handled via Modal's container image
# The inference server uses Modal's pre-built SGLang image
